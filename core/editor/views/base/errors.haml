- errors ||= []
- if errors.any?
  .flash.validation-errors
    %h3 Can't continue:
    %ul
      - errors.each do |field, errs|
        - errs.each do |err|
          %li
            = field
            = err
